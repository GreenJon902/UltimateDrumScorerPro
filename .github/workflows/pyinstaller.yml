name: Create Executable

on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [windows, linux]
    
    name: Create executable for ${{ matrix.os }}
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
      
    - name: Create executable
      uses: JackMcKew/pyinstaller-action-${{ matrix.os }}@main
      with:
        path: .
  
    - uses: actions/upload-artifact@v3
      with:
        name: UDSP-${{ matrix.os }}
        path: src/dist/${{ matrix.os }}
  
