from score import ScoreSectionStorage


def readScoreFromFile(path: str) -> list[ScoreSectionStorage]:
    pass


def saveScoreToFile(path: str, score: list[ScoreSectionStorage]):
    print(f"Saving score to {path}")
    for item in score:
        pass
